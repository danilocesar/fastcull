# Rust
/target
**/*.rs.bk

# Test data: large RAW files are fetched by testdata/fetch.sh, never committed
/testdata/raws/

# Local tooling
/.direnv/
*.swp

# Staging dir the CI workflow fills before uploading the Windows test build
/artifact/
