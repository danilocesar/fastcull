# specs/history — dead text, disposable

Each file here is a spec exactly as it stood before its 2026-09-17 rewrite
(brief 007): every superseded rule, gate finding, measurement and evidence
narrative, verbatim. Nothing live depends on this folder — the module specs
cite briefs, issues and commits — and the user intends to delete it once the
rewrite has settled: git log is the history.
